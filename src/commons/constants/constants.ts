export const HOSTS: {
  local: string;
  dev: string;
  sit: string;
  uat: string;
  prod: string;
} = {
  local: 'local host',
  dev: 'dev host',
  sit: 'sit host',
  uat: 'uat host',
  prod: 'prod host',
};
